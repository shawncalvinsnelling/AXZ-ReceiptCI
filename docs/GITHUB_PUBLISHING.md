# GitHub Publishing Checklist

Repository name:

```text
AXZ-ReceiptCI
```

Description:

```text
Deterministic CI receipt engine that verifies tests, hashes artifacts, and emits reproducible JSON release certificates.
```

Create repo with:

```text
Add README: Off
Add .gitignore: Off
License: None / Off
```

Upload all files, including hidden files:

```text
.github/workflows/verify.yml
.gitignore
```

Commit message:

```text
Add deterministic CI receipt engine
```

Release tag:

```text
v1.0.0
```

Release title:

```text
AXZ ReceiptCI v1.0.0 — deterministic CI receipt engine
```
